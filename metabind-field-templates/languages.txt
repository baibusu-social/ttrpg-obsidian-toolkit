INPUT[inlineSelect(
  options(null),
  options(common),
  options(draconic),
  options(dwarven),
  options(elven),
  options(fey),
  options(gnomish),
  options(goblin),
  options(halfling),
  options(jotun),
  options(orcish),
  options(sakvroth),
  options(aklo),
  options(chthonian),
  options(diabolic),
  options(empyrean),
  options(kholo),
  options(nectril),
  options(petran),
  options(pyric),
  options(shadowtongue),
  options(sussuran),
  options(thalassic),
  options(muan),
  options(talican)
)]